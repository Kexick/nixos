{
programs.nvf.settings.vim.terminal.toggleterm = {
  enable = true;
  mappings.open = "<leader>e";       # клавиша для открытия;
  setupOpts = {
    direction = "horizontal";
    size = 8;                     # высота терминала (8 строк)
  };
};
}
